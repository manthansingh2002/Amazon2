{
  "name": "backend",
  "type":"module",
  "version": "1.0.0",
  "description": "backend of amazon",
  "main": "Products.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch backend --exec node --experimental-modules backend/server.js"
  },
  "author": "Msk",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
